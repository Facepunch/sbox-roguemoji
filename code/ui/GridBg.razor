@using Sandbox;
@using Sandbox.UI;

@inherits Panel;
@namespace Interfacer

<style>
    .grid_bg 
    {
        opacity: 0.04;
        position:absolute;
        pointer-events:none;
        background-repeat: repeat;
        background-position: 0 0;
        background-size: 80px;
        border-radius:3px;
    }
</style>

<root class="grid_bg" style="width: @(40 * Hud.Instance.GridWidth)px; height: @(40 * Hud.Instance.GridHeight)px; background-image: url( textures/bg_tile.png );"></root>

@code
{
    
}